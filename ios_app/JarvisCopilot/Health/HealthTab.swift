import SwiftUI

/// The Health tab. Built out in full by the next change.
struct HealthTab: View {
    var body: some View {
        NavigationStack {
            Text("Health")
                .jcScreen("Health")
        }
    }
}
