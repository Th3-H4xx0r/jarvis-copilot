# Tasks — 2026-09-26 (in order)

Worked top to bottom. A box is ticked only when the change is shipped (phone
installed / server deployed) and checked. Re-read this list after every task.

- [ ] **1. INMO GO3 glasses: bare-bones support, ready for tomorrow.** "add
  minimal support as bare bones so that I can find and hook up the full
  functionality later … add the card in there along with a 3d model of the
  glasses and some controls on there as well with whatever is supported."
  Approved design: standard Bluetooth only (the GO3 pairs as a headset); no
  INMO control protocol.
  - Card on Devices → Wearables, always shown; joins the dashboard and the
    agent's device list once the glasses have connected.
  - Procedural 3D model (checked in offscreen renders before the phone).
  - Page: Talk to Jarvis, audio output picker, "Use ‹name› as my glasses",
    pairing help, rename, and a greyed "needs the glasses' control link"
    section (lens, touchpad & GO key, camera, battery).
  - ✅ Route watcher (`Glasses/GlassesAudioLink.swift`): finds the glasses
    on the iPhone's audio route by name ("INMO"/"GO3") or by the device you
    picked with "Use … as my glasses" (kept by MAC, so A2DP and HFP are one
    device). 10/10 matcher tests pass.
  - ✅ Card + page (`Glasses/InmoGo3View.swift`), roster + dashboard picture,
    `wearables_list` mentions the glasses; "connect" reports the route.
  - Found: at idle iOS only lists the glasses while they are the audio
    output (inputs need a recording session), so the card may read
    "Disconnected" while music plays through the iPhone speaker.
  - Found (Swift review): "GO 3" alone is the JBL Go 3 speaker's real name,
    so it would have been taken for the glasses, and Forget couldn't undo it.
    Now only a name with the word "INMO" is recognised, only while no pair
    is known; after that only that pair's MAC counts. Also fixed: the watcher
    starts on background launches too, "last seen" is stamped when the
    glasses leave the route, "Not on audio" instead of "Disconnected", pairing
    help and the claim card only before the glasses are known, and the card
    model only turns once they are.
  - Added (asked: "very nice and feature rich like the jarvis pod UI + ring
    UIs"): the page now follows the Pod page — hero with the model and status
    chips; Get started checklist that ticks itself (paired / heard Jarvis in
    them / talked through their mics); Sound (audio output picker, spoken
    speaker test); Talking to Jarvis ("Start Voice when they connect", the
    voice speech model); Device (Bluetooth name, A2DP/HFP, sample rate, audio
    delay, address — read live from iOS); tiles for what needs the control
    link. The card shows even before the glasses are ever paired.
  - ✅ Tests: 128/128 across the glasses, wearables, devices, dashboard and
    snapshot suites (the matcher's new JBL / known-pair / empty-uid tests
    were seen failing first).
  - 3D model v1 rendered: red bands, label plates, silver-ringed corner
    lenses, nose pads and ear bends match the photos, but the front read as a
    thin brow bar with wire rims.
  - ✅ 3D model v2: one solid matte-charcoal wayfarer front (8.5 mm brow,
    4.5 mm rims) with only a thin bright bevel, flattened-oval black temples
    with the red bands and label plates, grey-capped ear ends, green HUD when
    connected. Checked in offscreen renders (card, hero, front, side, top,
    turntable) against the photos before the phone. Not modelled: the
    bead-blast grain, "INMO" text on the label, the front's slight wrap.
  - ✅ Installed on the iPhone (Devices → Wearables → "Smart glasses").

## Malimite follow-up

- [x] Inspect https://github.com/LaurieWired/Malimite and clone its source into ignored `inmo-re/work/Malimite`.
- [x] Trace archive extraction → Ghidra import → DecompInterface; check encryption-related implementations.
- [x] Record pinned-commit evidence in `inmo-re/notes/malimite-evaluation.md`. No FairPlay decryption stage found; `KeyEncryption` handles AI API-key configuration.
- [ ] Meaningful INMO code decompilation still requires readable executable bytes. Malimite was source-audited, not built or run; no code-output success claimed.
