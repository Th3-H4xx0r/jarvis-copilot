# Tasks — 2026-09-24 (in order)

Worked top to bottom. A box is ticked only when the fix is shipped (server
deployed / phone installed / Mac client updated) and checked. Re-read this list
after every task.

- [x] **1. Mac Settings → Voices: a back button.** Opening a voice shows its
  transcript with no way back to the voice list.
  - ✅ A voice opens as a sheet (name, line count, Done / Esc) over the list.
- [x] **2. Mac Voice: stop listening at the end of a turn, and show words live.**
  - It keeps listening after you stop talking, with Soniox and with on-device.
  - With Soniox, no live text while speaking (on-device shows it; Live already
    streams Soniox words — do the same for Voice).
  - ✅ A pause far below how loud the turn was now ends it (a noisy room never
    reached the fixed silence level, so the turn ran to the 30 s cap).
  - ✅ The server forwards Soniox's words during the turn; the panel shows them.
- [x] **3. One place for settings.** Voice settings live only in the Settings
  window; the panel keeps just the chat-session chooser.
  - ✅ The model/settings chip is gone from the panel; Jarvis Settings holds them.
- [x] **4. Phone Live: better speaker identity.** After a Spanish clip, his own
  voice was matched to the Spanish speaker.
  - ✅ Found: both Soniox speakers in each stream were named "Me", and middling
    matches taught "Me" other people's voices (100 learned exemplars, most
    unlike his recordings) — so every voice matched "Me".
  - ✅ Two Soniox speakers in one stream can no longer share a voice (the closer
    keeps it, the other gets its own); only a strong match (≥ 0.62) teaches.
  - ✅ "Me" cleaned: 88 foreign voiceprints removed (backup on the server).
- [x] **5. Phone Live: voices list and merge.** The transcript shows two
  speakers who are both him, but Voices lists only "Me", so they can't be
  merged; the voice's transcript also misses his latest lines.
  - ✅ Found: "Speaker 8162" *was* "Me" — Me's own id (…8162) shown by its
    digits, because line frames never carried the voice's name. There was only
    one voice, so nothing to merge.
  - ✅ Every line frame now carries `speaker_name`; a voice's samples are its
    latest lines across sessions (they were ordered within a session).
- [x] **6. A full-transcript popup per person, paginated as you scroll** — on
  the phone, web and Mac, opened by tapping the voice.
  - ✅ Phone: tap a voice card → full-screen sheet (Done), 50 lines at a time.
  - ✅ Web: click a voice card → modal popup (✕ / Esc / backdrop); checked in a
    browser: 50 → 100 on scroll.
  - ✅ Mac: Settings → Voices → a voice opens as a sheet (task 1).
- [x] **7. Phone Voice on Soniox: live words and a fast turn.** Show Soniox's
  words as they come, and stop waiting for a full stop → transcribe → send.
  - ✅ Words: the server forwards Soniox's words during the turn (task 2).
  - ✅ Fast turn: Soniox's own end-of-speech detection is on for Voice; when it
    hears you stop, the server tells the device and the turn ends at once
    instead of waiting out the silence window (phone + Mac; on-device mode
    keeps deciding its own turn). Server + Mac shipped; phone installs when it
    next connects.
- [x] **8. Jarvis Pod: accuracy** as good as the phone and Mac.
  - ✅ Found: at 03:33Z a settings save set the server's Voice engine to local,
    and that one setting drove the browser AND the Pod, so the Pod fell back to
    the old local model. Replayed his last 8 Pod clips: Soniox beat local on
    each clear one.
  - ✅ The Pod now has its own engine (default Soniox) — task 10.
  - ✅ Soniox drifted into Slovak/Indonesian on short unclear clips; Voice now
    leans English when no languages are set (a hint, not a lock).
  - ✅ The Pod's mic caught Jarvis's own reply ("Very good, sir. I shall
    remain— Nothing."): leading words that run inside the last reply are
    dropped (3+ words), so "Nothing." is heard as the stop it was.
  - ⚠️ Not changed (firmware is his WIP): the Pod ends a turn after only 550 ms
    of quiet (`kEndSilenceMs`, phone uses 650–1100), which cut "I hope that
    you can hear me" at "you—".
- [x] **9. Wake word on Soniox?** Research the cost and whether it is a good
  idea. (Last, per his note: after everything else is fixed and pushed.)
  - **Verdict: not as the wake word itself.** Soniox real-time is $0.12/hour of
    open stream. Listening all day costs $2.88/day ≈ **$87/month per Pod**
    (about 100× his whole Soniox bill today, ~$0.50–0.80/month). Gating it on
    voice activity still bills every conversation in the room: ~4–6 h/day ≈
    $15–20/month. It is also slower (words must come back over the network,
    versus tens of ms on the chip), keeps Wi-Fi streaming around the clock, and
    sends the room's audio off the house all day.
  - **The real problem is the detector:** his notes say WakeNet at 0.55 caught
    about 1 "Hey Jarvis" in 8 (TTS-trained custom model).
  - **Recommended, in order:**
    1. Swap WakeNet for **microWakeWord's `hey_jarvis`**, a trained model that
       runs on the ESP32-S3 (inference under 10 ms, every 20 ms) and is one of
       its official launch models. Firmware work: TFLite Micro + the model.
    2. **Soniox as the second check, not the first:** run the on-chip detector
       at a low, sensitive threshold, send ~1.5 s of audio from before the
       trigger with the turn, and have the server drop the turn quietly unless
       its first words include "Jarvis". Billed only when the chip fires: even
       100 false triggers a day × 4 s ≈ $0.40/month.
  - Not built: both need Pod firmware (his WIP). Server side of (2) is small.
- [x] **10. Wearables screen: choose the Pod's speech model** (Soniox or the
  server's own).
  - ✅ New `pod` speech surface (default Soniox). Pod card → Voice & clock →
    "Speech model" menu; also a "Jarvis Pod" row in phone/web/Mac Speech settings.
- [x] **11. Pod recorded clips won't play on the phone** (OSStatus 561017449).
  - ✅ 561017449 = '!pri', insufficient priority: the clip player set its own
    audio category and activated it, around the arbiter that the keepalive,
    Voice and Live share, and iOS refused it while one of them held the session.
    Playback is now a claim on the arbiter (full-rate `.playback`, mixing; rides
    along under Voice/Live), released when the clip ends.
