# JarvisCopilot Web UI -- minimal Python dependencies
# The server uses only stdlib + pyyaml + wsproto.
# All heavy ML/agent deps live in the JarvisCopilot agent venv.
pyyaml>=6.0
# wsproto: sans-IO WebSocket protocol library used by the /api/voice/s2s/ws
# realtime voice endpoint. ~150KB, no transitive deps, well-tested. The
# webui's stdlib http.server has no native WebSocket support; wsproto handles
# the protocol state machine while we drive the raw socket ourselves.
wsproto>=1.2
